# Production dependencies (lightweight - no torch/detoxify)
# For full local development with all backends, use requirements.txt

# Core dependencies
future
emoji
requests
sqlalchemy==1.3.23 
python-telegram-bot==11.1.0
psycopg2-binary
feedparser

# WhatsApp bot dependencies
qrcode[pil]
Pillow

# AI moderation (OpenAI only in production)
openai
python-dotenv
